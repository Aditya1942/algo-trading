{
  "name": "algo-trading",
  "version": "1.0.0",
  "scripts": {
    "server": "cd server && bun --hot run index.ts",
    "client": "cd client && bun run dev",
    "build:client": "cd client && bun run build",
    "dev": "bun run --parallel server client"
  }
}
